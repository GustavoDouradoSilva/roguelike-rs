#[derive(Clone)]
pub struct Position {
    pub x: i32,
    pub y: i32,
}

pub struct Draw {
    pub ch: char,
    pub color: u32,
}

/*
pub struct Tile {
    pub name: String,
    //pub pos: Position,
    pub draw: Draw,
    pub walkable: bool,
}
*/
